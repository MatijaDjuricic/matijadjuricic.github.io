functions: 
	private void button6_Click(object sender, EventArgs e)
        {
            string duzina = textBox3.Text;
            if (duzina.Length <= 8)
            {
                errorProvider1.SetError(textBox3, "unesite ispravnu lozinku");
                textBox3.Clear();
                textBox3.Focus();
            }
            else
            {
                errorProvider1.SetError(textBox3, "");
            }
        }

        private void button7_Click(object sender, EventArgs e)
        {
            if (textBox3.UseSystemPasswordChar != true)
            {
                textBox3.UseSystemPasswordChar = true;
                button7.Text = "vidi sifru";
            }
            else 
            {
                textBox3.UseSystemPasswordChar = false;
                button7.Text = "sakri sifru";
            }
        }
progres bar:
	private void Form1_Load(object sender, EventArgs e)
        {
            tbIme.Enabled = true;
            tbPrezime.Enabled = false;
            tbMaticni.Enabled = false;
            tsProgressBar.Value = tsProgressBar.Minimum;
            tsLabel.Text = "";
        }

        private void tbIme_Leave(object sender, EventArgs e)
        {
            if (String.IsNullOrEmpty(tbIme.Text))
            {
                tsProgressBar.Value = tsProgressBar.Minimum;
            }
            else
            {
                tbIme.Enabled = false;
                tbPrezime.Enabled = true;
                tbMaticni.Enabled = false;
                tbPrezime.Focus();
            }
        }

        private void tbPrezime_Enter(object sender, EventArgs e)
        {
            tsProgressBar.Value = tsProgressBar.Maximum * 1 / 3;
            tsLabel.Text = "Korak 1 završen";
        }

        private void tbPrezime_Leave(object sender, EventArgs e)
        {
            if (String.IsNullOrEmpty(tbPrezime.Text))
            {
                tsProgressBar.Value = tsProgressBar.Maximum * 1 / 3;
            }
            else
            {
                tbIme.Enabled = false;
                tbPrezime.Enabled = false;
                tbMaticni.Enabled = true;
                tbMaticni.Focus();
            }
        }

        private void tbMaticni_Enter(object sender, EventArgs e)
        {
            tsProgressBar.Value = tsProgressBar.Maximum * 2 / 3;
            tsLabel.Text = "Korak 2 završen";
        }

        private void tbMaticni_Leave(object sender, EventArgs e)
        {
            if (String.IsNullOrEmpty(tbMaticni.Text))
            {
                tsProgressBar.Value = tsProgressBar.Maximum * 2 / 3;
            }
            else
            {
                tbIme.Enabled = false;
                tbPrezime.Enabled = false;
                tbMaticni.Enabled = false;
                tsProgressBar.Value = tsProgressBar.Maximum;
                tsLabel.Text = "Korak 3 završen";
            }

        }
        private void resetuj()
        {
            tbIme.Clear();
            tbPrezime.Clear();
            tbMaticni.Clear();
            tbIme.Enabled = true;
            tbPrezime.Enabled = false;
            tbMaticni.Enabled = false;
            tsProgressBar.Value = tsProgressBar.Minimum;
            tsLabel.Text = "";
        }
        private void button1_Click(object sender, EventArgs e)
        {
            if (String.IsNullOrEmpty(tbIme.Text) || String.IsNullOrEmpty(tbPrezime.Text) || String.IsNullOrEmpty(tbMaticni.Text))
            {
                MessageBox.Show("Sva polja su obavezna", "Greška",
                MessageBoxButtons.OK, MessageBoxIcon.Error);
            }
            else
            {
                DialogResult rezultat = MessageBox.Show("Da li ste sigurni?", "Pitanje",
                MessageBoxButtons.YesNo, MessageBoxIcon.Question);
                if (rezultat == DialogResult.Yes)
                {
                    MessageBox.Show("Uspešna registracija!", "Informacija",
                    MessageBoxButtons.OK, MessageBoxIcon.Information);
                }
            }
        }

        private void button2_Click(object sender, EventArgs e)
        {
            DialogResult rezultat = MessageBox.Show("Da li ste sigurni?", "Pitanje",
            MessageBoxButtons.YesNo, MessageBoxIcon.Question);
            if (rezultat == DialogResult.Yes)
            {
                resetuj();
            }
        }
Login: 
private void button1_Click(object sender, EventArgs e)
        {
            if (tbKorisnickoIme.Text == "admin")
            {
                if (tbLozinka.Text == "123")
                {
                    MessageBox.Show("Uspešno ste se prijavili", "Obaveštenje",
                    MessageBoxButtons.OK, MessageBoxIcon.Information);
                }
                else
                {
                    MessageBox.Show("Uneli ste netačnu lozinku", "GREŠKA",
                    MessageBoxButtons.OK, MessageBoxIcon.Error);
                    tbLozinka.Clear();
                    tbLozinka.Focus();
                }
            }
            else
            {
                MessageBox.Show("Uneli ste netačno korisničko ime", "GREŠKA",
                MessageBoxButtons.OK, MessageBoxIcon.Error);
                tbKorisnickoIme.Clear();
                tbKorisnickoIme.Focus();
            }
        }

        private void button2_Click(object sender, EventArgs e)
        {
            tbKorisnickoIme.Clear();
            tbLozinka.Clear();

        }

        private void checkBox1_CheckedChanged(object sender, EventArgs e)
        {
            if (checkBox1.Checked)
            {
                tbLozinka.UseSystemPasswordChar = false;
            }
            else
            {
                tbLozinka.UseSystemPasswordChar = true;
            }

        }
Validacija unosa:

        private void Form1_Load(object sender, EventArgs e)
        {

        }

        private void button1_Click(object sender, EventArgs e)
        {
            if (Information.IsNumeric(tbBroj.Text) == false)
            {
                errorProvider1.SetError(tbBroj, "Uneti numeričku vrednost!");
                tbBroj.Focus();
                tbBroj.SelectAll();
            }
            else
            {
                errorProvider1.SetError(tbBroj, "");
            }
        }

        private void button2_Click(object sender, EventArgs e)
        {
            if (Information.IsDate(tbDatum.Text) == false)
            {
                errorProvider1.SetError(tbDatum, "Uneti datumsku vrednost!");
                tbDatum.Focus();
                tbDatum.SelectAll();
            }
            else
            {
                errorProvider1.SetError(tbDatum, " ");
            }
        }

        private void tbAutomatic_KeyPress(object sender, KeyPressEventArgs e)
        {
            if (Char.IsDigit(e.KeyChar) || e.KeyChar == '\b')
            {
                e.Handled = false;
            }
            else
            {
                e.Handled = true;
            }
        }

        private void tbValidacija_Validating(object sender, CancelEventArgs e)
        {
            if (String.IsNullOrEmpty(tbValidacija.Text))
            {
                errorProvider1.SetError(tbValidacija, "Uneti vrednost");
                button3.Enabled = false;
            }
            else
            {
                errorProvider1.SetError(tbValidacija, "");
                button3.Enabled = true;
            }

        }
files:
 private void Form1_Load(object sender, EventArgs e)
        {
            folderBrowserDialog1.RootFolder = Environment.SpecialFolder.MyComputer;
            saveFileDialog1.InitialDirectory = @"C:\Users\Korisnik\Desktop";
            openFileDialog1.InitialDirectory = @"C:\Users\Korisnik\Desktop";
            richTextBox1.Font = new Font("Calibri", 11);
        }

        private void button1_Click(object sender, EventArgs e)
        {
            if (folderBrowserDialog1.ShowDialog() == DialogResult.OK)
            {
                saveFileDialog1.InitialDirectory = folderBrowserDialog1.SelectedPath;
                openFileDialog1.InitialDirectory = folderBrowserDialog1.SelectedPath;
            }
        }

        private void button2_Click(object sender, EventArgs e)
        {
            saveFileDialog1.Filter = "rtf fajlovi|*.rtf";
            if (saveFileDialog1.ShowDialog() == DialogResult.OK)
            {
                richTextBox1.SaveFile(saveFileDialog1.FileName);
                MessageBox.Show("Podaci sacuvani");
                richTextBox1.Clear();
            }
            else
            {
                MessageBox.Show("Nije dozvoljeno cuvanje praznog fajla");
            }
        }

        private void button3_Click(object sender, EventArgs e)
        {
            openFileDialog1.Filter = "rtf fajlovi|*.rtf";
            if (openFileDialog1.ShowDialog() == DialogResult.OK)
            {
                richTextBox1.LoadFile(openFileDialog1.FileName);
               
            }

        }

        private void button4_Click(object sender, EventArgs e)
        {
            if (richTextBox1.SelectedText.Length > 0)
            {
                if (colorDialog1.ShowDialog() == DialogResult.OK)
                {
                    richTextBox1.SelectionColor = colorDialog1.Color;
                }
                else
                {
                    MessageBox.Show("Odaberite tekst koji zelite da obojite");
                }
            }
	}
---------------------------
Dialogs: 
private void button1_Click(object sender, EventArgs e)
        {
            if (openFileDialog1.ShowDialog() == DialogResult.OK)
            {
                richTextBox1.LoadFile(openFileDialog1.FileName);
            }
        }

        private void button2_Click(object sender, EventArgs e)
        {
            if (saveFileDialog1.ShowDialog() == DialogResult.OK)
            {
                richTextBox1.SaveFile(saveFileDialog1.FileName);
            }
        }

        private void button3_Click(object sender, EventArgs e)
        {
            if (colorDialog1.ShowDialog() == DialogResult.OK)
            {
                richTextBox1.SelectionColor = colorDialog1.Color;
            }
        }

        private void button4_Click(object sender, EventArgs e)
        {
            if (fontDialog1.ShowDialog() == DialogResult.OK)
            {
                richTextBox1.SelectionFont = fontDialog1.Font;
            }
        }
